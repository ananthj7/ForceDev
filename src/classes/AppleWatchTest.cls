@isTest
private class AppleWatchTest {
	
	@isTest static void initOpty() {
		Opportunity Opty = new Opportunity();
		opty.name = 'Swift Deal';
		Opty.StageName = 'Prospecting';
		Opty.CloseDate = Date.today();
		// Implement test code
	}
	
	
}